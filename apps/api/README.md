# @dinamic/api

NestJS modular monolith com 14 bounded contexts: `auth`, `tenants`, `properties`, `leads`, `visits`, `proposals`, `contracts`, `rental`, `financial`, `conversations`, `ai-agent`, `demand-signals`, `notifications`, `lgpd`, `audit`.

**Status:** placeholder. Implementação na **task #10** do backlog (NestJS shell + módulos vazios + adapters stub).

**Stack futura:**
- NestJS 10
- Prisma 5 (via `@dinamic/db`)
- BullMQ
- OpenAPI 3.1 (auto-gerado)
- Adapters: OpenRouter (ativo), Asaas, Evolution, D4Sign, Mapbox, R2 (adiados)

Ver detalhes em `docs/workflow/squads/02-backend.md`.
